# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/
.env/

# Environment files (never commit secrets)
.env
.env.*
*.local

# Test / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Model weights (large binaries — download separately via scripts/download_model.py)
models/*.pth
models/*.pt
models/*.bin
models/*.pkl
models/hf/
# but DO track the shipped LoRA replay adapter (small: 3 MB)
!models/replay_lora/
# Codecfake weights: CC BY-NC-ND upstream — do not redistribute (download separately)
models/codecfake/

# Evidence packages (audit exports — never committed).
# Anchored to repo root so it does NOT match the source package
# voiceshield/evidence/.
/evidence/

# Calibration recordings (raw audio — never committed)
/eval_recordings/
eval_set/

# Audio fixtures (large binaries — generate/obtain separately)
tests/fixtures/*.wav
tests/fixtures/*.mp3
tests/fixtures/*.flac

# Node / frontend
Interface/node_modules/
Interface/dist/
Interface/.vite/
*.tsbuildinfo
.npm/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Logs produced at runtime
logs/
*.log

# Antigravity / agent tooling
.agents/.runtime/

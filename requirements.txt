numpy>=1.24.0,<2.5  # numba (via librosa) does not support numpy 2.5 yet
scipy>=1.11.0
sounddevice>=0.4.6
soundfile>=0.12.1
librosa>=0.10.1
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.40.0
pillow>=10.0.0
fastapi>=0.103.0
uvicorn>=0.23.2
websockets>=11.0.3
python-multipart>=0.0.9
typer>=0.9.0

# Stage 2 optional detectors (graceful-degrade if absent):
silero-vad>=6.0        # neural VAD (MIT)
speechbrain>=1.0       # ECAPA-TDNN speaker-drift (Apache-2.0)
audioseal>=0.1.4       # AI-watermark detection (MIT)
omegaconf>=2.1         # audioseal config loader needs OmegaConf.resolve
